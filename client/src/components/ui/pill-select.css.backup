.pill-select-container {
  --pill-bg: #ffffff;
  --base: #000000;
  --hover-text: #ffffff;
  --pill-text: #000000;
  --error-color: #ef4444;
  --pill-pad-x: 16px;
  width: 100%;
}

/* Label */
.pill-select-label {
  display: block;
  margin-bottom: 8px;
  font-size: 14px;
  font-weight: 500;
  color: var(--pill-text);
}

.required-indicator {
  color: var(--error-color);
  margin-left: 2px;
}

/* Select Container */
.pill-select {
  position: relative;
  width: 100%;
}

/* Size Variants */
.pill-select-sm .pill-select-button {
  height: 32px;
  padding: 0 12px;
  font-size: 13px;
}

.pill-select-md .pill-select-button {
  height: 40px;
  padding: 0 16px;
  font-size: 14px;
}

.pill-select-lg .pill-select-button {
  height: 48px;
  padding: 0 20px;
  font-size: 15px;
}

/* Button */
.pill-select-button {
  width: 100%;
  background: var(--base);
  color: var(--hover-text);
  border: 2px solid var(--base);
  border-radius: 9999px;
  font-weight: 500;
  cursor: pointer;
  position: relative;
  overflow: hidden;
  display: flex;
  align-items: center;
  justify-content: space-between;
  transition: none;
  outline: none;
}

.pill-select-button:hover:not(:disabled) {
  background: var(--base);
}

.pill-select-button:focus-visible {
  outline: 2px solid var(--pill-bg);
  outline-offset: 2px;
}

.pill-select-button:disabled {
  opacity: 0.5;
  cursor: not-allowed;
}

.pill-select.has-error .pill-select-button {
  border-color: var(--error-color);
}

/* Hover Circle */
.hover-circle {
  position: absolute;
  left: 50%;
  top: 50%;
  width: 200%;
  height: 200%;
  border-radius: 50%;
  background: rgba(255, 255, 255, 0.1);
  pointer-events: none;
  z-index: 0;
}

/* Button Content */
.button-content {
  display: flex;
  align-items: center;
  justify-content: space-between;
  width: 100%;
  position: relative;
  z-index: 1;
  gap: 8px;
}

.option-icon {
  display: flex;
  align-items: center;
  flex-shrink: 0;
}

.selected-value {
  flex: 1;
  text-align: left;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.selected-value.is-placeholder {
  opacity: 0.6;
}

.chevron-icon {
  flex-shrink: 0;
  margin-left: auto;
  transition: none;
}

/* Dropdown Menu */
.pill-select-menu {
  position: absolute;
  top: calc(100% + 8px);
  left: 0;
  right: 0;
  background: var(--base);
  border: 2px solid var(--base);
  border-radius: 20px;
  box-shadow: 0 10px 40px rgba(0, 0, 0, 0.15);
  list-style: none;
  margin: 0;
  padding: 4px;
  z-index: 1000;
  max-height: 280px;
  overflow-y: auto;
  opacity: 0;
  visibility: hidden;
}

.pill-select-menu::-webkit-scrollbar {
  width: 6px;
}

.pill-select-menu::-webkit-scrollbar-track {
  background: transparent;
}

.pill-select-menu::-webkit-scrollbar-thumb {
  background: rgba(255, 255, 255, 0.2);
  border-radius: 3px;
}

.pill-select-menu::-webkit-scrollbar-thumb:hover {
  background: rgba(255, 255, 255, 0.3);
}

/* Menu Items */
.pill-select-item {
  padding: 8px 12px;
  margin: 2px 0;
  background: var(--pill-bg);
  color: var(--pill-text);
  border-radius: 9999px;
  cursor: pointer;
  font-size: 13px;
  font-weight: 500;
  transition: none;
  display: flex;
  align-items: center;
  gap: 8px;
  position: relative;
  overflow: hidden;
}

.pill-select-item:hover {
  background: rgba(255, 255, 255, 0.9);
}

.pill-select-item.is-selected {
  background: var(--pill-bg);
  font-weight: 600;
}

.item-icon {
  display: flex;
  align-items: center;
  flex-shrink: 0;
}

.item-label {
  flex: 1;
}

.selected-icon {
  margin-left: auto;
  flex-shrink: 0;
  color: var(--pill-text);
}

/* Error State */
.pill-select-error {
  display: block;
  margin-top: 4px;
  font-size: 12px;
  color: var(--error-color);
}

/* Disabled State */
.pill-select.is-disabled {
  pointer-events: none;
}

.pill-select.is-disabled .pill-select-button {
  background: rgba(0, 0, 0, 0.05);
  border-color: rgba(0, 0, 0, 0.1);
  color: rgba(0, 0, 0, 0.3);
}

/* Dark Mode Support */
@media (prefers-color-scheme: dark) {
  .pill-select-container {
    --pill-bg: #1a1a1a;
    --base: #ffffff;
    --hover-text: #000000;
    --pill-text: #ffffff;
  }

  .pill-select-button {
    background: var(--base);
    color: var(--hover-text);
    border-color: var(--base);
  }

  .pill-select-menu {
    background: var(--base);
    border-color: var(--base);
    box-shadow: 0 10px 40px rgba(0, 0, 0, 0.5);
  }

  .pill-select-item {
    background: var(--pill-bg);
    color: var(--pill-text);
  }

  .pill-select-item:hover {
    background: rgba(26, 26, 26, 0.9);
  }

  .hover-circle {
    background: rgba(0, 0, 0, 0.1);
  }

  .pill-select-menu::-webkit-scrollbar-thumb {
    background: rgba(0, 0, 0, 0.3);
  }

  .pill-select-menu::-webkit-scrollbar-thumb:hover {
    background: rgba(0, 0, 0, 0.4);
  }

  .pill-select.is-disabled .pill-select-button {
    background: rgba(255, 255, 255, 0.05);
    border-color: rgba(255, 255, 255, 0.1);
    color: rgba(255, 255, 255, 0.3);
  }
}

/* Theme variations for modals/cards */
.dark .pill-select-container,
[data-theme="dark"] .pill-select-container {
  --pill-bg: #1a1a1a;
  --base: #ffffff;
  --hover-text: #000000;
  --pill-text: #ffffff;
}